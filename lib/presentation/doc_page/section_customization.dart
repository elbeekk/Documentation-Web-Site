import 'package:flutter/material.dart';

class SectionCustomization extends StatelessWidget {
  const SectionCustomization({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
